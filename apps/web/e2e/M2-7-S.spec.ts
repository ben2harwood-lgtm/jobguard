import{expect,test}from"@playwright/test";import{openReview}from"./helpers/capture-journey";test.setTimeout(180_000);const V=async(page:any,id:string,value:string)=>expect.poll(async()=>page.getByTestId(id).textContent(),{timeout:45_000}).toBe(value);const X=async(page:any,text:string)=>expect(page.getByText(text,{exact:true})).toBeVisible();test("keeps mandatory work visible and explains advisory relevance",async({page})=>{await openReview(page);await X(page,"Your inbox is clear");await page.getByRole("button",{name:"Seed relevance scenario"}).click();await V(page,"inbox-mandatory-decision-count","2");await page.getByRole("button",{name:"Set advisory budget to zero"}).click();await V(page,"visible-advisory-count","0");await X(page,"3 advisory checks available");await expect(page.getByText("Electrical proof required",{exact:true})).toBeVisible();await page.getByRole("button",{name:"Why am I seeing this?"}).first().click();await X(page,"Why am I seeing this?");await expect(page.getByText("Rule:",{exact:false})).toBeVisible();await expect(page.getByText("Practice sandbox — synthetic data; nothing is sent or charged",{exact:true})).toHaveCount(1);await page.screenshot({path:`test-results/M2-7-S-${test.info().project.name}.png`,fullPage:true})});
