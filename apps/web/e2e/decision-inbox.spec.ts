import{test,expect}from"@playwright/test";
import{openReview}from"./helpers/capture-journey";
test("Decision Inbox lists, details, and dismisses an advisory without authorization",async({page})=>{await openReview(page);const jobId=new URL(page.url()).pathname.split("/").pop()!;const seeded=await page.request.post("/api/decisions",{data:{version:"finding-evaluation-command.v1",commandId:crypto.randomUUID(),tenantId:"11111111-1111-4111-8111-111111111111",jobId}});expect(seeded.status()).toBe(200);await page.goto("/");await page.getByRole("button",{name:/Decisions/}).first().click();await expect(page.getByRole("heading",{name:"Decision Inbox"})).toBeVisible();await expect(page.getByTestId("mandatory-decision-count")).toHaveText("1");await expect(page.getByTestId("advisory-decision-count")).toHaveText("1");await page.getByRole("button",{name:/Check the materials bill/}).click();await expect(page.getByText(/REVIEW SUGGESTION/)).toBeVisible();await expect(page.getByText(/No overcharge has been detected/)).toBeVisible();await page.getByRole("button",{name:"Dismiss suggestion"}).click();await expect(page.getByText("Dismissed — no action authorized")).toBeVisible();await page.getByRole("button",{name:/Required proof missing/}).click();await expect(page.getByText(/Mandatory blockers cannot be suppressed/)).toBeVisible();});
