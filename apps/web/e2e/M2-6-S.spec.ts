import{expect,test}from"@playwright/test";import{openReview}from"./helpers/capture-journey";test.setTimeout(180_000);const V=async(page:any,id:string,value:string)=>expect.poll(async()=>page.getByTestId(id).textContent(),{timeout:45_000}).toBe(value);const X=async(page:any,text:string)=>expect(page.getByText(text,{exact:true})).toBeVisible();test("shows readiness risks and requires an explicit revised plan",async({page})=>{await openReview(page);await page.getByRole("button",{name:"Record generated readiness scenario"}).click();await X(page,"This work is not ready");await X(page,"2 units still needed");await X(page,"Crew availability unknown");await V(page,"readiness-recovery-fee","£0.00");await page.getByRole("button",{name:"Advance scenario clock"}).click();await V(page,"readiness-due-review-count","1");await V(page,"readiness-external-effects","0");await page.getByRole("button",{name:"Confirm revised plan"}).click();await V(page,"readiness","Ready on the recorded facts");await expect(page.getByText("synthetic",{exact:false}).first()).toBeVisible();await expect(page.getByText("Practice sandbox — synthetic data; nothing is sent or charged",{exact:true})).toHaveCount(1);await page.screenshot({path:`test-results/M2-6-S-${test.info().project.name}.png`,fullPage:true})});
