import{expect,test}from"@playwright/test";
async function open(page:import("@playwright/test").Page){await page.goto("/");await page.getByRole("button",{name:"Continue with demo code"}).click();await page.getByRole("button",{name:"Walk a new job"}).click();}
test("walk-it shows source beside five-plus cited suggestions and an unknown rate",async({page})=>{await open(page);await page.getByRole("button",{name:"Create draft proposal"}).click();const proposal=page.getByLabel("Cited proposal");await expect(proposal.locator("li")).toHaveCount(6);await expect(proposal).toContainText("Rate unknown");await expect(proposal).toContainText("Confirm disposal");await expect(proposal).toContainText("Draft only · no canonical scope · not sent");await proposal.getByText(/Stored source/).click();await expect(proposal).toContainText("14 King Street");});
test("invalid provider output preserves original for retry",async({page})=>{await page.route("**/api/jobs/capture",route=>route.fulfill({status:422,contentType:"application/json",body:JSON.stringify({code:"INVALID_MODEL_OUTPUT",originalInputRetained:true})}));await open(page);const source=page.getByLabel("Source text");await source.fill("JOB: My original\nITEM: Safe work");await page.getByRole("button",{name:"Create draft proposal"}).click();await expect(page.getByRole("status")).toContainText("original text is still here");await expect(source).toHaveValue(/My original/u);});
