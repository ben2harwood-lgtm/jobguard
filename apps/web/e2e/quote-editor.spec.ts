import{expect,test}from"@playwright/test";
import{openQuote}from"./helpers/capture-journey";
test("builder prices a synthetic quote and sees exact live coverage",async({page})=>{await openQuote(page);await expect(page.getByRole("heading",{name:"Price the work"})).toBeVisible();await expect(page.getByLabel("Tax policy")).toBeDisabled();await expect(page.getByLabel("Coverage diff")).toContainText("Blocking questions: 0");for(const input of await page.getByLabel(/Unit rate/u).all())if(await input.inputValue()==="")await input.fill("100.00");await expect(page.getByRole("heading",{name:"Live totals"})).toBeVisible();await page.getByRole("button",{name:"Save draft revision"}).click();await expect(page.getByText(/Draft revision 1 saved/u)).toContainText("Draft revision 1 saved");await expect(page.getByText(/nothing was sent or billed/u)).toBeVisible();});
