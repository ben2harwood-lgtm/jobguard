import{expect,test}from"@playwright/test";
test("issues one synthetic invoice and projects partial, paid, reversed and credited history",async({page})=>{await page.goto("/");await page.getByRole("button",{name:"Continue with demo code"}).click();await page.getByRole("button",{name:"Draft final account"}).click();await expect(page.getByText(/D02, D06 and G1 remain off/)).toBeVisible();await page.getByRole("button",{name:/Approve synthetic issue/}).click();await expect(page.getByText(/Invoice SYN-2026-000001/)).toBeVisible();await expect(page.getByRole("heading",{name:"Payment: unpaid"})).toBeVisible();await page.getByRole("button",{name:"Record £750 payment"}).click();await expect(page.getByRole("heading",{name:"Payment: partial"})).toBeVisible();await page.getByRole("button",{name:"Record remaining £750"}).click();await expect(page.getByRole("heading",{name:"Payment: paid"})).toBeVisible();await page.getByRole("button",{name:"Reverse latest receipt"}).click();await expect(page.getByText(/latest receipt reversed/)).toBeVisible();await page.getByRole("button",{name:"Issue full credit note"}).click();await expect(page.getByRole("heading",{name:"Payment: credited"})).toBeVisible();await expect(page.getByText(/original invoice unchanged/)).toBeVisible();await page.screenshot({path:"/tmp/jobguard-customer-invoice.png",fullPage:true})});
