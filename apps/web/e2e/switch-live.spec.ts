import{expect,test,type Page}from"@playwright/test";
async function click(page:Page,name:string){const button=page.getByRole("button",{name,exact:true});await expect(button).toBeVisible();await expect(button).toBeEnabled();await button.click();}
import{openQuote}from"./helpers/capture-journey";
test("switch live freezes the accepted baseline in the no-charge pilot",async({page})=>{await openQuote(page);for(const input of await page.getByLabel(/Unit rate/u).all())if(await input.inputValue()==="")await input.fill("100.00");await click(page,"Save draft revision");await click(page,"Preview immutable quote");await click(page,"Simulate sending this quote");await expect(page.getByTestId("quote-delivery")).toHaveText("Queued — not sent");await click(page,"Continue fake worker");await expect(page.getByTestId("quote-delivery")).toHaveText("Simulated delivery — nothing sent");await click(page,"Record practice acceptance");await click(page,"Start this practice job");const live=page.getByLabel("Live activation");await expect(live).toContainText("No charge");await expect(live).toContainText("no JobGuard fee, obligation, journal, or collection request");await expect(live).toContainText("Illustrative only until D01 approval");await expect(live).toContainText("£79.00");await expect(live).toContainText("No real charge or collection");await expect(page.getByRole("button",{name:"Cancel pre-live acceptance"})).toBeDisabled();});
