import {cookies} from "next/headers";import {NextResponse} from "next/server";import {SandboxServiceError} from "@jobguard/api/workspace";import {workspaceApplication} from "../../../../lib/workspace-server";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{return NextResponse.json(await workspaceApplication().sandbox.get((await cookies()).get("jg_session")?.value,(await params).id))}catch(e){const code=e instanceof SandboxServiceError?e.code:"DATABASE_UNAVAILABLE";return NextResponse.json({code},{status:code==="UNAUTHENTICATED"?401:code==="NOT_FOUND"?404:503})}}
