import {cookies} from "next/headers";import {NextResponse} from "next/server";import {SandboxServiceError} from "@jobguard/api/workspace";import {workspaceApplication} from "../../../../../lib/workspace-server";
export async function POST(request:Request,{params}:{params:Promise<{id:string;action:string}>}){try{const{id,action}=await params,session=(await cookies()).get("jg_session")?.value,body=await request.json();const app=workspaceApplication().sandbox;if(action!=="reset"&&action!=="archive"&&action!=="advance")return NextResponse.json({code:"NOT_FOUND"},{status:404});return NextResponse.json(await app[action](session,id,body));}catch(e){const code=e instanceof SandboxServiceError?e.code:"DATABASE_UNAVAILABLE";return NextResponse.json({code},{status:code==="UNAUTHENTICATED"?401:code==="NOT_FOUND"?404:code==="ARCHIVED"||code==="COMMAND_CONFLICT"?409:code==="INVALID_COMMAND"?400:503})}}
