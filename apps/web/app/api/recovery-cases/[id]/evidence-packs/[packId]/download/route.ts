import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../../../lib/workspace-server";export async function GET(_r:Request,{params}:{params:Promise<{id:string;packId:string}>}){if(!hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});const{id,packId}=await params;try{const body=await workspaceApplication().evidencePacks.download(id,packId);return new NextResponse(body,{headers:{"content-type":"application/zip","content-disposition":`attachment; filename="jobguard-evidence-pack-${packId}.zip"`}})}catch(e){return NextResponse.json({code:e instanceof Error?e.message:"NOT_FOUND"},{status:404})}}
