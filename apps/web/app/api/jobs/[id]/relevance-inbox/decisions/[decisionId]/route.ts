import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../../../lib/workspace-server";export async function POST(r:Request,{params}:{params:Promise<{id:string;decisionId:string}>}){if(!hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{const{id,decisionId}=await params;return NextResponse.json(await workspaceApplication().inboxRelevance.dismiss(id,decisionId,await r.json()))}catch(e){const code=e instanceof Error?e.message:"INVALID_INBOX";return NextResponse.json({code},{status:code.includes("NOT_FOUND")?404:400})}}
