import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../lib/workspace-server";
async function auth(){return hasSyntheticSession((await cookies()).get("jg_session")?.value)}
export async function GET(_r:Request,{params}:{params:Promise<{id:string}>}){if(!await auth())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().fees.read((await params).id))}catch(e){return NextResponse.json({code:e instanceof Error?e.message:"NOT_FOUND"},{status:404})}}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!await auth())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});const body=await request.json().catch(()=>undefined);if(body===undefined)return NextResponse.json({code:"INVALID_JSON"},{status:400});try{return NextResponse.json(await workspaceApplication().fees.create((await params).id,body))}catch(e){return NextResponse.json({code:e instanceof Error?e.message:"INVALID_COMMAND"},{status:400})}}
