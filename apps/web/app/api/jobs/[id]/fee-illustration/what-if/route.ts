import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../../lib/workspace-server";
export async function POST(request:Request){if(!hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});const body=await request.json().catch(()=>undefined);if(body===undefined)return NextResponse.json({code:"INVALID_JSON"},{status:400});try{return NextResponse.json(workspaceApplication().fees.whatIf(body))}catch{return NextResponse.json({code:"INVALID_COMMAND"},{status:400})}}
