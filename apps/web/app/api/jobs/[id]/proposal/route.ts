import {cookies} from "next/headers";import {NextResponse}from"next/server";import{CaptureApplicationError}from"@jobguard/api/workspace";import{hasSyntheticSession}from"../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../lib/workspace-server";
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){if(!hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().capture.get((await params).id))}catch(e){const code=e instanceof CaptureApplicationError?e.code:"DATABASE_UNAVAILABLE";return NextResponse.json({code},{status:code==="NOT_FOUND"?404:503})}}
