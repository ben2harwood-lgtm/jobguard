import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../lib/workspace-server";
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){if(!await hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().value.read((await params).id))}catch(error){const code=error instanceof Error?error.message:"DATABASE_UNAVAILABLE";return NextResponse.json({code},{status:code==="JOB_NOT_FOUND"?404:503})}}
