import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../lib/workspace-server";
const auth=async()=>hasSyntheticSession((await cookies()).get("jg_session")?.value);export async function GET(_r:Request,{params}:{params:Promise<{id:string}>}){if(!await auth())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().materials.view((await params).id))}catch(e){return NextResponse.json({code:e instanceof Error?e.message:"DATABASE_UNAVAILABLE"},{status:400})}}export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){if(!await auth())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().materials.addRequirement((await params).id,await r.json()))}catch(e){const code=e instanceof Error?e.message:"INVALID_COMMAND";return NextResponse.json({code},{status:code.includes("STALE")?409:400})}}
