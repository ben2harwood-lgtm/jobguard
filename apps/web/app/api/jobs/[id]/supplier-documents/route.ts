import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../lib/workspace-server";export async function GET(_r:Request,{params}:{params:Promise<{id:string}>}){if(!hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().supplierDocuments.view((await params).id))}catch(e){return NextResponse.json({code:e instanceof Error?e.message:"DATABASE_UNAVAILABLE"},{status:400})}}
