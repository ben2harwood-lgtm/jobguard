import{cookies}from"next/headers";import{NextResponse}from"next/server";import{CommercialIntegrityApplication}from"@jobguard/api/commercial-integrity";import{hasSyntheticSession}from"../../../../../lib/synthetic-server";
const singleton=globalThis as typeof globalThis&{integrityApp?:CommercialIntegrityApplication};const app=singleton.integrityApp??=new CommercialIntegrityApplication("synthetic_demo");
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if(!hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(app.review((await params).id,await request.json()),{headers:{"Cache-Control":"no-store"}})}catch(e){return NextResponse.json({code:e instanceof Error?e.message:"INVALID_COMMAND"},{status:400})}}
