import{cookies}from"next/headers";import{NextResponse}from"next/server";import{QuoteApplicationError}from"@jobguard/api/workspace";import{hasSyntheticSession}from"../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../lib/workspace-server";
const auth=async()=>hasSyntheticSession((await cookies()).get("jg_session")?.value);
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){if(!await auth())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().quote.get((await params).id))}catch(e){return NextResponse.json({code:e instanceof QuoteApplicationError?e.code:"DATABASE_UNAVAILABLE"},{status:503})}}
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const body=await request.json().catch(()=>null);if(!await auth())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().quote.save((await params).id,body))}catch(e){const code=e instanceof QuoteApplicationError?e.code:"DATABASE_UNAVAILABLE";return NextResponse.json({code},{status:code==="QUOTE_REVISION_CONFLICT"?409:code==="INVALID_QUOTE"?400:503})}}
