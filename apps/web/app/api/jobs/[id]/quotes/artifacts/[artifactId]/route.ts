import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../../../lib/workspace-server";
export async function GET(_request:Request,{params}:{params:Promise<{id:string;artifactId:string}>}){if(!hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{const{id,artifactId}=await params,x=await workspaceApplication().quote.pdf(id,artifactId);return new NextResponse(x.pdf,{headers:{"content-type":"application/pdf","content-disposition":`attachment; filename=quote-v${x.version}.pdf`,"x-jobguard-content-hash":x.hash,"x-jobguard-document-version":String(x.version)}});}catch{return NextResponse.json({code:"NOT_FOUND"},{status:404});}}
