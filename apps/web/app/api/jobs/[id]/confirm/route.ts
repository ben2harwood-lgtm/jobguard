import {cookies} from "next/headers";import {NextResponse}from"next/server";import{CaptureApplicationError}from"@jobguard/api/workspace";import{hasSyntheticSession}from"../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../lib/workspace-server";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const body=await request.json().catch(()=>null);if(!hasSyntheticSession((await cookies()).get("jg_session")?.value))return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{return NextResponse.json(await workspaceApplication().capture.confirm((await params).id,body))}catch(e){const code=e instanceof CaptureApplicationError?e.code:"DATABASE_UNAVAILABLE";return NextResponse.json({code},{status:code==="REVIEW_REVISION_CONFLICT"||code==="COMMAND_CONFLICT"?409:code==="DATABASE_UNAVAILABLE"?503:400})}}
