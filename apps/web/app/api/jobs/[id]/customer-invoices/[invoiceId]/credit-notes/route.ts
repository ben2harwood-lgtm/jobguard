import{cookies}from"next/headers";import{NextResponse}from"next/server";import{hasSyntheticSession}from"../../../../../../lib/synthetic-server";import{workspaceApplication}from"../../../../../../lib/workspace-server";const auth=async()=>hasSyntheticSession((await cookies()).get("jg_session")?.value);
export async function GET(_r:Request,{params}:{params:Promise<{id:string;invoiceId:string}>}){if(!await auth())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{const{id,invoiceId}=await params;return NextResponse.json(await workspaceApplication().customerInvoice.creditView(id,invoiceId))}catch(e){return NextResponse.json({code:e instanceof Error?e.message:"DATABASE_UNAVAILABLE"},{status:404})}}
export async function POST(r:Request,{params}:{params:Promise<{id:string;invoiceId:string}>}){if(!await auth())return NextResponse.json({code:"UNAUTHENTICATED"},{status:401});try{const{id,invoiceId}=await params;return NextResponse.json(await workspaceApplication().customerInvoice.issueCredit(id,{...await r.json(),invoiceId}))}catch(e){const code=e instanceof Error?e.message:"DATABASE_UNAVAILABLE";return NextResponse.json({code},{status:code.includes("STALE")||code.includes("EXCEEDS")?409:400})}}
