import report from "../../../../docs/fixtures/things-to-check/held-out-v1.json";
const percent=(n:number)=>`${(n*100).toFixed(1)}%`;
export default function ThingsToCheckReport(){return <main className="workspace-page"><header><p className="eyebrow">Frozen synthetic evaluation</p><h1>Things to check held-out report</h1><p>This versioned output measures confirmed-fact matching only. It is not live OCR accuracy or G2 pilot approval.</p></header><dl><dt>Output version</dt><dd data-testid="report-version">{report.version}</dd><dt>Frozen cases</dt><dd data-testid="held-out-case-count">{report.corpusSize}</dd><dt>False discovery rate</dt><dd>{percent(report.metrics.falseDiscoveryRate)} (denominator {report.denominators.actionablePredictions} actionable monetary Decisions)</dd><dt>Recall at GBP 25 or more</dt><dd>{percent(report.metrics.recall)} (denominator {report.denominators.labelledDiscrepanciesAtLeastGbp25} labelled discrepancies)</dd><dt>Critical fixtures</dt><dd>{report.criticalFixtures.passed} / {report.criticalFixtures.total} passed</dd><dt>Zero-prediction failure</dt><dd>{report.zeroPredictionFailure?"Failed":"Passed"}</dd></dl><h2>Class counts</h2><pre>{JSON.stringify(report.classCounts,null,2)}</pre><h2>Confusion matrix</h2><pre>{JSON.stringify(report.confusionMatrix,null,2)}</pre><h2>Errors</h2><pre>{JSON.stringify(report.errors,null,2)}</pre><h2>Separate extraction result</h2><p>{report.endToEndExtraction.status}: {report.endToEndExtraction.reason}</p><p>Actual output SHA-256: <code>{report.outputSha256}</code></p></main>}
