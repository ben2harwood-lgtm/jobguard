"use client";import{useCallback,useEffect,useState}from"react";
type Order={draftId:string;revisionId:string;revision:number;quantity:string;unitPricePence:number;orderNetPence:number;agreedNetPence:number;differencePence:number;recipient:string;requiredDate:string;authorityHash:string;status:string;outboxEffectCount:number;availability:string;preventedFeePence:number};
const pounds=(p:number)=>`${p<0?"−":""}£${(Math.abs(p)/100).toLocaleString("en-GB",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
export function PurchaseOrder({jobId}:{jobId:string}){const[order,setOrder]=useState<Order|null>(null),[requirementId,setRequirementId]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const load=useCallback(async()=>{const[m,o]=await Promise.all([fetch(`/api/jobs/${jobId}/materials`,{cache:"no-store"}),fetch(`/api/jobs/${jobId}/purchase-orders`,{cache:"no-store"})]);if(m.ok)setRequirementId((await m.json()).materials[0]?.id??"");if(o.ok)setOrder((await o.json()).order)},[jobId]);useEffect(()=>{void load()},[load]);useEffect(()=>{if(requirementId)return;const timer=setInterval(()=>{void load()},1500);return()=>clearInterval(timer)},[requirementId,load]);async function revise(form:FormData){setBusy(true);setError("");try{const price=Math.round(Number(form.get("price"))*100),r=await fetch(`/api/jobs/${jobId}/purchase-orders/revisions`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({version:"purchase-order-draft.v1",requirementId,quantity:String(form.get("quantity")),unitPricePence:price,recipient:String(form.get("recipient")),requiredDate:String(form.get("requiredDate")),expectedRevision:order?.revision??0})});if(!r.ok)throw new Error((await r.json()).code);await load()}catch(e){setError(e instanceof Error?e.message:"Could not preview order")}finally{setBusy(false)}}async function place(){if(!order)return;setBusy(true);setError("");try{const commandId=crypto.randomUUID(),r=await fetch(`/api/jobs/${jobId}/purchase-orders/placement`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({version:"purchase-order-placement.v1",command:{version:"command.v1",commandId,commandType:"purchase_order.simulate",semanticKey:`purchase-order:${order.draftId}`,actorMembershipId:"d1500000-0000-4000-8000-000000000003",subjectType:"purchase_order_revision",subjectRef:order.revisionId,action:{actionType:"purchase_order.simulate",recipient:order.recipient,contentHash:order.authorityHash,aggregateRevision:order.revision,amountPence:order.orderNetPence,currency:"GBP",policyVersion:"synthetic-po.v1",expiresAt:"2099-01-01T00:00:00.000Z"}}})});if(!r.ok)throw new Error((await r.json()).code);await load()}catch(e){setError(e instanceof Error?e.message:"Authority changed — re-confirm this order")}finally{setBusy(false)}}return <section id="purchase-orders" className="materials-panel"><h2>Check a purchase order</h2><p>Use the supplied fictional order only. No supplier is contacted and no order is placed.</p>{!requirementId?<p>Save a material and agreed price before drafting an order.</p>:<form action={revise}><label>Quantity<input name="quantity" inputMode="decimal" defaultValue={order?.quantity??"10"} required/></label><label>Order unit price (£)<input name="price" inputMode="decimal" defaultValue={order?(order.unitPricePence/100).toFixed(2):"25.00"} required/></label><label>Fictional recipient<input name="recipient" type="email" defaultValue={order?.recipient??"orders@fictional-merchant.invalid"} required/></label><label>Required date<input name="requiredDate" type="date" defaultValue={order?.requiredDate??"2026-10-01"} required/></label><button className="primary" disabled={busy}>{order?"Preview changed order":"Preview proposed order"}</button></form>}{error&&<p role="alert" tabIndex={-1}>{error}</p>}{order&&<article><p data-testid="purchase-order-availability">{order.availability}</p><dl><dt>Proposed order net</dt><dd data-testid="order-proposed-net">{pounds(order.orderNetPence)}</dd><dt>Recorded agreed net</dt><dd data-testid="order-agreed-net">{pounds(order.agreedNetPence)}</dd><dt>Price difference</dt><dd data-testid="order-price-difference">{pounds(order.differencePence)}</dd></dl>{order.differencePence>0&&<p>This order is {pounds(order.differencePence)} above the recorded agreed price</p>}<p data-testid="order-status">{order.status}</p>{order.status.startsWith("Simulated")&&<><p data-testid="order-net">{pounds(order.orderNetPence)}</p><p data-testid="prevented-fee">{pounds(order.preventedFeePence)}</p></>}<button className="primary" type="button" onClick={()=>void place()} disabled={busy||order.differencePence!==0}>Approve simulated order</button><p data-testid="purchase-order-outbox-effects">{order.outboxEffectCount}</p></article>}<button type="button" onClick={()=>void load()}>Refresh purchase order</button></section>}
