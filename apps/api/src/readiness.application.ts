import type{Pool}from"pg";import{DEMO_MEMBERSHIP_ID,DEMO_TENANT_ID,ReadinessRepository,verifiedTenantContextFromMembership}from"@jobguard/db";import{readinessAdvanceV1,readinessPlanV1}from"./readiness.contracts.js";const context=()=>verifiedTenantContextFromMembership({identityUserId:"d1500000-0000-4000-8000-000000000001",membershipId:DEMO_MEMBERSHIP_ID,tenantId:DEMO_TENANT_ID}as any);export class ReadinessApplication{private repo;constructor(pool:Pool){this.repo=new ReadinessRepository(pool)}view(jobId:string){return this.repo.view(context(),jobId)}record(jobId:string,raw:unknown){return this.repo.record(context(),jobId,readinessPlanV1.parse(raw))}advance(jobId:string,raw:unknown){return this.repo.advance(context(),jobId,readinessAdvanceV1.parse(raw))}}
