import type{Pool}from"pg";import{DEMO_MEMBERSHIP_ID,DEMO_TENANT_ID,MaterialRepository,verifiedTenantContextFromMembership}from"@jobguard/db";import{materialRateCommandV1,materialRequirementCommandV1}from"./material.contracts.js";
const context=()=>verifiedTenantContextFromMembership({identityUserId:"d1500000-0000-4000-8000-000000000001",membershipId:DEMO_MEMBERSHIP_ID,tenantId:DEMO_TENANT_ID}as any);
export class MaterialApplication{private repo;constructor(pool:Pool){this.repo=new MaterialRepository(pool)}addRate(raw:unknown){return this.repo.addRate(context(),materialRateCommandV1.parse(raw))}addRequirement(jobId:string,raw:unknown){return this.repo.addRequirement(context(),{jobId,...materialRequirementCommandV1.parse(raw)})}async view(jobId:string){return{version:"material-workspace.v1",environment:"synthetic_demo",jobId,asOf:new Date().toISOString().slice(0,10),materials:await this.repo.view(context(),jobId,new Date().toISOString().slice(0,10)),realExternalActions:0}}}
