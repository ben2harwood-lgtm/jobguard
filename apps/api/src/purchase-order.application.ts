import type{Pool}from"pg";import{consequentialCommandV1Schema,DEMO_MEMBERSHIP_ID,DEMO_TENANT_ID,PurchaseOrderRepository,verifiedTenantContextFromMembership}from"@jobguard/db";import{purchaseOrderDraftV1,purchaseOrderPlacementV1}from"./purchase-order.contracts.js";
const context=()=>verifiedTenantContextFromMembership({identityUserId:"d1500000-0000-4000-8000-000000000001",membershipId:DEMO_MEMBERSHIP_ID,tenantId:DEMO_TENANT_ID}as any);
export class PurchaseOrderApplication{private repo;constructor(pool:Pool){this.repo=new PurchaseOrderRepository(pool)}async view(jobId:string){return{version:"purchase-order-workspace.v1",environment:"synthetic_demo",jobId,order:await this.repo.view(context(),jobId),realExternalActions:0}}revise(jobId:string,raw:unknown){return this.repo.revise(context(),jobId,purchaseOrderDraftV1.parse(raw))}place(jobId:string,raw:unknown){return this.repo.place(context(),jobId,consequentialCommandV1Schema.parse(purchaseOrderPlacementV1.parse(raw).command))}}
