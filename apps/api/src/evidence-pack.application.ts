import type{Pool}from"pg";import{DEMO_IDENTITY_USER_ID,DEMO_MEMBERSHIP_ID,DEMO_TENANT_ID,EvidencePackRepository,verifiedTenantContextFromMembership}from"@jobguard/db";import{evidencePackCommandV1}from"./evidence-pack.contracts.js";const context=()=>verifiedTenantContextFromMembership({identityUserId:DEMO_IDENTITY_USER_ID,membershipId:DEMO_MEMBERSHIP_ID,tenantId:DEMO_TENANT_ID}as any);export class EvidencePackApplication{private repo;constructor(pool:Pool){this.repo=new EvidencePackRepository(pool)}async list(caseId:string){return{version:"evidence-pack-response.v1",claim:"mapped, inspectable",packs:await this.repo.list(context(),caseId)}}async generate(caseId:string,raw:unknown){await this.repo.generate(context(),caseId,evidencePackCommandV1.parse(raw));return this.list(caseId)}download(caseId:string,packId:string){return this.repo.download(context(),caseId,packId)}}
