import type{Pool}from"pg";import{DEMO_MEMBERSHIP_ID,DEMO_TENANT_ID,SupplierDocumentRepository,verifiedTenantContextFromMembership}from"@jobguard/db";import{goodsReceiptV1,supplierDocumentIntakeV1}from"./supplier-document.contracts.js";const context=()=>verifiedTenantContextFromMembership({identityUserId:"d1500000-0000-4000-8000-000000000001",membershipId:DEMO_MEMBERSHIP_ID,tenantId:DEMO_TENANT_ID}as any);export class SupplierDocumentApplication{private repo;constructor(pool:Pool){this.repo=new SupplierDocumentRepository(pool)}async view(jobId:string){return{version:"supplier-document-workspace.v1",environment:"synthetic_demo",jobId,state:await this.repo.view(context(),jobId)}}intake(jobId:string,raw:unknown){return this.repo.intake(context(),jobId,supplierDocumentIntakeV1.parse(raw))}receipt(jobId:string,raw:unknown){return this.repo.appendReceipt(context(),jobId,goodsReceiptV1.parse(raw))}}
