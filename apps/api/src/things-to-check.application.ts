import type{Pool}from"pg";import{DEMO_MEMBERSHIP_ID,DEMO_TENANT_ID,DiscrepancyRepository,verifiedTenantContextFromMembership}from"@jobguard/db";import{evaluateThingsV1,reviewThingV1,supersedeBillV1}from"./things-to-check.contracts.js";const context=()=>verifiedTenantContextFromMembership({identityUserId:"d1500000-0000-4000-8000-000000000001",membershipId:DEMO_MEMBERSHIP_ID,tenantId:DEMO_TENANT_ID}as any);export class ThingsToCheckApplication{private repo;constructor(pool:Pool){this.repo=new DiscrepancyRepository(pool)}async view(jobId:string){return{version:"things-to-check-workspace.v1",environment:"synthetic_demo",jobId,state:await this.repo.view(context(),jobId)}}evaluate(jobId:string,raw:unknown){return this.repo.evaluate(context(),jobId,evaluateThingsV1.parse(raw))}review(jobId:string,raw:unknown){return this.repo.review(context(),jobId,reviewThingV1.parse(raw))}supersede(jobId:string,raw:unknown){return this.repo.supersede(context(),jobId,supersedeBillV1.parse(raw))}}
