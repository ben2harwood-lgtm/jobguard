import type{Pool}from"pg";import{DEMO_MEMBERSHIP_ID,DEMO_TENANT_ID,SupplierMatchRepository,verifiedTenantContextFromMembership}from"@jobguard/db";import{supplierMatchCorrectionV1,supplierMatchCreateV1}from"./supplier-match.contracts.js";const context=()=>verifiedTenantContextFromMembership({identityUserId:"d1500000-0000-4000-8000-000000000001",membershipId:DEMO_MEMBERSHIP_ID,tenantId:DEMO_TENANT_ID}as any);export class SupplierMatchApplication{private repo;constructor(pool:Pool){this.repo=new SupplierMatchRepository(pool)}async view(jobId:string){return{version:"supplier-match-workspace.v1",environment:"synthetic_demo",jobId,state:await this.repo.view(context(),jobId)}}create(jobId:string,raw:unknown){const input=supplierMatchCreateV1.parse(raw);return this.repo.create(context(),jobId,input)}correct(jobId:string,raw:unknown){return this.repo.correct(context(),jobId,supplierMatchCorrectionV1.parse(raw))}}
