import{z}from"zod";export const inboxSeedV1=z.object({version:z.literal("inbox-seed.v1"),commandId:z.string().uuid()}).strict();export const inboxBudgetV1=z.object({version:z.literal("inbox-budget.v1"),commandId:z.string().uuid(),advisoryBudget:z.number().int().min(0).max(100)}).strict();export const inboxDismissV1=z.object({version:z.literal("inbox-dismiss.v1"),commandId:z.string().uuid()}).strict();
