# Synthetic local development infrastructure only.
services:
  postgres:
    image: postgres:16.10-alpine
    environment:
      POSTGRES_USER: jobguard
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: jobguard
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data", "./tools/docker/001-worker-roles.sql:/docker-entrypoint-initdb.d/001-worker-roles.sql:ro"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jobguard"]
      interval: 5s
      timeout: 5s
      retries: 10
  minio:
    image: minio/minio:RELEASE.2025-07-23T15-54-02Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: jobguard
      MINIO_ROOT_PASSWORD: devsecret123
    ports: ["9000:9000", "9001:9001"]
    volumes: ["miniodata:/data"]
  minio-init:
    image: minio/mc:RELEASE.2025-07-21T05-28-08Z
    depends_on: [minio]
    restart: "no"
    entrypoint: ["/bin/sh", "-c"]
    # Private and versioned, deliberately without Object Lock/WORM until retention approval.
    command: ["mc alias set local http://minio:9000 jobguard devsecret123 && mc mb --ignore-existing local/jobguard-evidence-private && mc anonymous set none local/jobguard-evidence-private && mc version enable local/jobguard-evidence-private"]
  mailhog:
    image: mailhog/mailhog:v1.0.1
    ports: ["1025:1025", "8025:8025"]
  worker:
    image: node:24.15.0-alpine
    working_dir: /workspace
    command: sh -c "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @jobguard/api start:worker"
    volumes: [".:/workspace"]
    environment:
      GRAPHILE_DATABASE_URL: postgresql://jobguard_infrastructure_login:dev-infrastructure-only@postgres:5432/jobguard
      RUNTIME_DATABASE_URL: postgresql://jobguard_runtime_login:dev-runtime-only@postgres:5432/jobguard
    depends_on:
      postgres:
        condition: service_healthy
volumes:
  pgdata: {}
  miniodata: {}
