# Local development only. Copy to .env and never commit real credentials.
API_PORT=3001
NEXT_PUBLIC_API_URL=http://localhost:3001
DATABASE_URL=postgresql://jobguard:dev@localhost:5432/jobguard
# Synthetic Vercel demo only (never put real credentials in this file):
# JOBGUARD_ENV=synthetic_demo
# DATABASE_URL=postgresql://jobguard_runtime:<runtime-password>@<neon-pooled-host>/jobguard_synthetic_demo?sslmode=require
# MIGRATION_DATABASE_URL=postgresql://<neon-owner>:<owner-password>@<neon-direct-host>/jobguard_synthetic_demo?sslmode=require
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY_ID=jobguard
S3_SECRET_ACCESS_KEY=devsecret123
SMTP_URL=smtp://localhost:1025
S3_REGION=eu-west-2
S3_BUCKET=jobguard-evidence-private
