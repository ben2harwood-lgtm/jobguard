import type{Pool}from"pg";import{DEMO_TENANT_ID}from"./demo-seed.js";import{withTenant,type VerifiedTenantContext}from"./tenant-context.js";
const context={tenantId:DEMO_TENANT_ID}as VerifiedTenantContext;
export class FeeIllustrationRepository{constructor(private pool:Pool){}async create(jobId:string,commandId:string){await withTenant(this.pool,context,db=>db.$client.query("SELECT app.create_fee_illustration_source($1,$2)",[jobId,commandId]));return this.read(jobId)}async read(jobId:string){return withTenant(this.pool,context,async db=>{const q=await db.$client.query<any>(`SELECT accepted_net_pence,eligible_net_pence,gross_landed_pence,base_obligation_pence,base_settled_pence,prior_posting_pence,statement_issued_count,ledger_entry_count,source_ids FROM app.fee_illustration_source WHERE tenant_id=$1 AND job_id=$2`,[DEMO_TENANT_ID,jobId]);if(!q.rowCount)return null;const x=q.rows[0];return{acceptedNetPence:Number(x.accepted_net_pence),eligibleNetPence:Number(x.eligible_net_pence),grossLandedPence:Number(x.gross_landed_pence),baseObligationPence:Number(x.base_obligation_pence),baseSettledPence:Number(x.base_settled_pence),priorPostingPence:Number(x.prior_posting_pence),statementIssuedCount:x.statement_issued_count,ledgerEntryCount:x.ledger_entry_count,sourceIds:x.source_ids as string[]}})}}
