import{createHash,randomUUID}from"node:crypto";import type{Pool}from"pg";import{appendAuditBatch}from"./audit.js";import{withTenant,type VerifiedTenantContext}from"./tenant-context.js";
export class PracticeInvoiceRepository{constructor(readonly pool:Pool){}private map(row:any){const total=Number(row.total_pence),credited=Number(row.credited_pence??0),paid=Number(row.paid_pence??0),debt=Math.max(0,total-credited);return{id:row.id,number:row.invoice_number,issuedOn:new Date(row.issued_on).toISOString().slice(0,10),recipient:"practice-customer@example.invalid",pdfSha256:row.pdf_sha256.trim(),finalAccountRevisionId:row.final_account_revision_id,sourceHash:row.source_hash.trim(),netPence:Number(row.net_pence),taxPence:Number(row.tax_pence),totalPence:total,balancePence:Math.max(0,debt-paid),customerCreditPence:Math.max(0,paid-debt),delivery:"simulated_delivery" as const}}
 async list(context:VerifiedTenantContext,jobId:string){return withTenant(this.pool,context,async db=>(await db.$client.query<any>(`SELECT i.*,COALESCE((SELECT sum(c.amount_pence) FROM app.customer_credit_note c WHERE(c.tenant_id,c.invoice_id)=(i.tenant_id,i.id)),0) credited_pence,COALESCE((SELECT sum(p.amount_pence) FROM app.customer_payment p WHERE(p.tenant_id,p.invoice_id)=(i.tenant_id,i.id)),0) paid_pence FROM app.customer_invoice i WHERE i.tenant_id=$1 AND i.job_id=$2 ORDER BY i.created_at,i.id`,[context.tenantId,jobId])).rows.map(row=>this.map(row)))}
 async issue(context:VerifiedTenantContext,input:{jobId:string;finalAccountRevisionId:string;actorMembershipId:string;commandId:string;recipient:string;issuedOn:string}){return withTenant(this.pool,context,async db=>{const r=(await db.$client.query<any>(`SELECT * FROM app.issue_practice_customer_invoice($1,$2,$3,$4,$5,$6,$7::date)`,[context.tenantId,input.jobId,input.finalAccountRevisionId,input.actorMembershipId,input.commandId,input.recipient,input.issuedOn])).rows[0];await appendAuditBatch(db,[{id:randomUUID(),version:"audit.v1",actorRef:`membership:${input.actorMembershipId}`,eventType:"customer_invoice.synthetic_issued",subjectType:"job",subjectRef:input.jobId,payload:{references:{invoiceId:r.invoice_id,finalAccountRevisionId:input.finalAccountRevisionId},hashes:{pdfSha256:r.pdf_sha256,sourceHash:r.source_hash},classifications:{action:"commercial",delivery:"operational"}}}]);return this.map((await db.$client.query<any>(`SELECT * FROM app.customer_invoice WHERE tenant_id=$1 AND id=$2`,[context.tenantId,r.invoice_id])).rows[0])})}
 async artifact(context:VerifiedTenantContext,jobId:string,invoiceId:string){return withTenant(this.pool,context,async db=>(await db.$client.query<{pdf_bytes:Buffer}>(`SELECT pdf_bytes FROM app.customer_invoice WHERE tenant_id=$1 AND job_id=$2 AND id=$3`,[context.tenantId,jobId,invoiceId])).rows[0]?.pdf_bytes??null)}
 async creditView(context:VerifiedTenantContext,jobId:string,invoiceId:string){return withTenant(this.pool,context,async db=>{const invoice=(await db.$client.query<any>(`SELECT i.*,COALESCE((SELECT sum(c.amount_pence) FROM app.customer_credit_note c WHERE(c.tenant_id,c.invoice_id)=(i.tenant_id,i.id)),0) credited_pence,COALESCE((SELECT sum(p.amount_pence) FROM app.customer_payment p WHERE(p.tenant_id,p.invoice_id)=(i.tenant_id,i.id)),0) paid_pence FROM app.customer_invoice i WHERE i.tenant_id=$1 AND i.job_id=$2 AND i.id=$3`,[context.tenantId,jobId,invoiceId])).rows[0];if(!invoice)throw new Error("INVOICE_NOT_FOUND");const credits=(await db.$client.query<any>(`SELECT * FROM app.customer_credit_note WHERE tenant_id=$1 AND job_id=$2 AND invoice_id=$3 ORDER BY created_at,id`,[context.tenantId,jobId,invoiceId])).rows.map(c=>({id:c.id,number:c.credit_number,netPence:Number(c.net_pence),taxPence:Number(c.tax_pence),totalPence:Number(c.total_pence),reason:c.reason,pdfSha256:c.pdf_sha256.trim(),approvedAt:new Date(c.approved_at).toISOString(),sourceInvoiceHash:c.source_invoice_hash.trim()}));return{invoice:this.map(invoice),credits}})}
 async previewCredit(context:VerifiedTenantContext,input:{jobId:string;invoiceId:string;netPence:number;reason:string}){return withTenant(this.pool,context,async db=>{const row=(await db.$client.query<any>(`SELECT i.pdf_sha256,i.invoice_number,i.total_pence,COALESCE((SELECT sum(c.amount_pence) FROM app.customer_credit_note c WHERE(c.tenant_id,c.invoice_id)=(i.tenant_id,i.id)),0) credited FROM app.customer_invoice i WHERE i.tenant_id=$1 AND i.job_id=$2 AND i.id=$3`,[context.tenantId,input.jobId,input.invoiceId])).rows[0];if(!row)throw new Error("INVOICE_NOT_FOUND");const tax=Math.floor((input.netPence+2)/5),gross=input.netPence+tax;if(gross>Number(row.total_pence)-Number(row.credited))throw new Error("CREDIT_EXCEEDS_AVAILABLE_AMOUNT");return{previewHash:createHash("sha256").update(`${input.jobId}${input.invoiceId}${input.netPence}${row.credited}${row.pdf_sha256.trim()}`).digest("hex"),invoiceNumber:row.invoice_number,netPence:-input.netPence,taxPence:-tax,totalPence:-gross,reason:input.reason,availablePence:Number(row.total_pence)-Number(row.credited)}})}
 async issueCredit(context:VerifiedTenantContext,input:{jobId:string;invoiceId:string;actorMembershipId:string;commandId:string;netPence:number;reason:string;previewHash:string}){return withTenant(this.pool,context,async db=>{const row=(await db.$client.query<any>(`SELECT * FROM app.issue_practice_customer_credit_note($1,$2,$3,$4,$5,$6,$7,$8)`,[context.tenantId,input.jobId,input.invoiceId,input.actorMembershipId,input.commandId,input.netPence,input.reason,input.previewHash])).rows[0],seen=(await db.$client.query(`SELECT 1 FROM app.audit_event WHERE tenant_id=$1 AND event_type='customer_credit_note.synthetic_approved' AND payload->'references'->>'creditNoteId'=$2`,[context.tenantId,row.credit_note_id])).rowCount;if(!seen)await appendAuditBatch(db,[{id:randomUUID(),version:"audit.v1",actorRef:`membership:${input.actorMembershipId}`,eventType:"customer_credit_note.synthetic_approved",subjectType:"job",subjectRef:input.jobId,payload:{references:{creditNoteId:row.credit_note_id,invoiceId:input.invoiceId,commandId:input.commandId},hashes:{pdfSha256:row.pdf_sha256,previewHash:input.previewHash},classifications:{action:"commercial",environment:"operational"}}}]);return{creditNoteId:row.credit_note_id,creditNumber:row.credit_number,pdfSha256:row.pdf_sha256.trim(),totalPence:Number(row.total_pence)}})}
}
