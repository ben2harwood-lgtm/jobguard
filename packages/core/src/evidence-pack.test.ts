import{describe,expect,it}from"vitest";import{buildEvidenceManifest,canonicalManifest,inspectEvidenceManifest,manifestDigest,sha256}from"./evidence-pack.js";
const source=(overrides={})=>({sourceId:"quote",kind:"accepted_quote" as const,version:1,label:"Accepted quote",content:"£320.00",jobId:"job-1",...overrides});
describe("evidence pack domain engine",()=>{it("uses SHA-256 and deterministic ordering",()=>{expect(sha256("abc")).toBe("ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad");const a=buildEvidenceManifest("case","job-1",[source({sourceId:"z"}),source({sourceId:"a",kind:"approval"})]);const b=buildEvidenceManifest("case","job-1",[source({sourceId:"a",kind:"approval"}),source({sourceId:"z"})]);expect(canonicalManifest(a)).toBe(canonicalManifest(b));expect(manifestDigest(a)).toBe(manifestDigest(b))});it("reports each failure independently and lineage hashes differ",()=>{const original=source(),redacted=source({sourceId:"quote-redacted",content:"£[redacted]",redactedFrom:{sourceId:"quote",version:1,hash:sha256("£320.00")}}),m=buildEvidenceManifest("case","job-1",[original,redacted]);expect(m.entries[0]?.contentHash).not.toBe(m.entries[1]?.contentHash);expect(inspectEvidenceManifest(m,[source({content:"changed"}),redacted],false).findings).toEqual(expect.arrayContaining(["Content hash mismatch","Checkpoint not independently trusted"]));expect(inspectEvidenceManifest(m,[redacted],true).findings).toContain("Missing original source");expect(inspectEvidenceManifest(m,[source({version:2}),redacted],true).findings).toContain("Wrong source version")})});
