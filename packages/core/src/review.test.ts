import { describe, expect, it } from "vitest";
import { assertReviewConfirmable, reconciliationSummary, ReviewIncompleteError, type ProposalReview } from "./review.js";
const id=(n:number)=>`${String(n).padStart(8,"0")}-0000-4000-8000-000000000000`;
const base:ProposalReview={version:"proposal_review_v1",reviewId:id(1),proposalId:id(2),jobId:id(3),revision:0,lines:[{id:id(4),scopeItemId:id(5),origin:"proposal",description:"Fit units",room:"Kitchen",category:"Joinery",quantity:"1",unit:"item",unitPricePence:null,disposition:"accepted",dismissalReason:null,parentScopeItemIds:[],sourceExcerpt:"Fit base cabinets"},{id:id(6),scopeItemId:id(7),origin:"proposal",description:"Old line",room:"Kitchen",category:"Other",quantity:null,unit:null,unitPricePence:null,disposition:"dismissed",dismissalReason:"Duplicate",parentScopeItemIds:[],sourceExcerpt:"old line"},{id:id(8),scopeItemId:id(9),origin:"human",description:"Protect floor",room:"Kitchen",category:"Preparation",quantity:"1",unit:"item",unitPricePence:null,disposition:"accepted",dismissalReason:null,parentScopeItemIds:[],sourceExcerpt:null}],questions:[{id:id(10),question:"Confirm disposal",blocking:false,disposition:"carry_to_quote",answer:null}]};
describe("proposal review",()=>{it("reports distinct reconciliation categories without a completeness claim",()=>expect(reconciliationSummary(base)).toEqual({proposed:2,accepted:2,dismissed:1,splitOrMerged:0,humanAdded:1}));it("requires an explicit disposition for every question",()=>expect(()=>assertReviewConfirmable({...base,questions:[{...base.questions[0]!,disposition:"unresolved"}]})).toThrow(ReviewIncompleteError));it("allows unknown rates for M1-4 but requires scope quantity and unit",()=>{expect(()=>assertReviewConfirmable(base)).not.toThrow();expect(()=>assertReviewConfirmable({...base,lines:[{...base.lines[0]!,quantity:null}]})).toThrow(/quantity/u);});});
